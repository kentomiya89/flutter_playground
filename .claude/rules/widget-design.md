---
description: このプロジェクトにおけるFlutterウィジェット設計の規約。
---

# ウィジェット設計のルール

- **`StatelessWidget` を優先**する。ミュータブルな状態が必要な場合のみ `StatefulWidget` を使う
- **`const` コンストラクタ**をできる限り使う（パフォーマンス向上）
- **1ファイル1サンプル** — 各 `main_*.dart` はサンプル間で状態・インポートを共有しない独立したファイル
- コントローラーを使う場合は **`dispose()` で必ず解放する**
